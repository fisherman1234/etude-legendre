<%= form_for(@type_document) do |f| %>
  <% if @type_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_document.errors.count, "error") %> prohibited this type_document from being saved:</h2>

      <ul>
      <% @type_document.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :parametres_cabinet_id %><br />
    <%= f.text_field :parametres_cabinet_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
