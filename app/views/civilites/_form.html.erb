<%= form_for(@civilite) do |f| %>
  <% if @civilite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@civilite.errors.count, "error") %> prohibited this civilite from being saved:</h2>

      <ul>
      <% @civilite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
