<%= form_for(@type_intervenant) do |f| %>
  <% if @type_intervenant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_intervenant.errors.count, "error") %> prohibited this type_intervenant from being saved:</h2>

      <ul>
      <% @type_intervenant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
