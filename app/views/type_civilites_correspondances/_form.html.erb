<%= form_for(@type_civilites_correspondance) do |f| %>
  <% if @type_civilites_correspondance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_civilites_correspondance.errors.count, "error") %> prohibited this type_civilites_correspondance from being saved:</h2>

      <ul>
      <% @type_civilites_correspondance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :parametres_cabinet_id %><br />
    <%= f.text_field :parametres_cabinet_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
