<div id="parameter-content">
  <%= netzke :items,
    :class_name => "Basepack::GridPanel",
    :model => "Item",
    :strong_default_attrs => {:parametres_cabinet_id => current_user.parametres_cabinet_id},
    :columns => [:description, {:name => "categorie__description"}, :prix_unitaire, {:name => "unite__description"}, {:name => "taux_tva__description"} ],
    :scope => {:parametres_cabinet_id => current_user.parametres_cabinet_id},
    :view_config => {
        :force_fit => true # force the columns to occupy all the available width
      },
     
    :enable_edit_in_form => false
    #:prohibit_update => true,
    #:prohibit_delete => true,
    #:prohibit_create => true
  
  %>
</div>