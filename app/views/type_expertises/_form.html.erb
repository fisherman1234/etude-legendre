<%= form_for(@type_expertise) do |f| %>
  <% if @type_expertise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_expertise.errors.count, "error") %> prohibited this type_expertise from being saved:</h2>

      <ul>
      <% @type_expertise.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :commentaires %><br />
    <%= f.text_area :commentaires %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
