<%= form_for(@type_activite) do |f| %>
  <% if @type_activite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_activite.errors.count, "error") %> prohibited this type_activite from being saved:</h2>

      <ul>
      <% @type_activite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
